# Интеграция Resend для отправки форм

## 📧 Настройка отправки писем через Resend

Проект настроен для отправки заявок с форм через Resend API. Письма будут приходить на `info@msk.splitis.ru`.

## 🔧 Установка зависимостей

```bash
npm install
```

Или установите Resend отдельно:

```bash
npm install resend
```

## 🔑 Настройка переменных окружения

### Для VPS Timeweb (рекомендуется)

**Вариант 1: PHP скрипт (проще всего)**

1. Скопируйте файл `api/contact.php` на сервер в директорию выше `_site`:
   ```bash
   /var/www/msk.splitis.ru/api/contact.php
   ```

2. Настройте переменные окружения в PHP (или отредактируйте файл напрямую):
   
   **Способ 1: Через .htaccess (если используется Apache):**
   ```apache
   SetEnv RESEND_API_KEY re_RfkBrEkZ_7gYxeyyNDhtR6AvYTQBmem3j
   SetEnv CONTACT_EMAIL info@msk.splitis.ru
   ```
   
   **Способ 2: Через php.ini или .user.ini:**
   ```ini
   RESEND_API_KEY=re_RfkBrEkZ_7gYxeyyNDhtR6AvYTQBmem3j
   CONTACT_EMAIL=info@msk.splitis.ru
   ```
   
   **Способ 3: Отредактируйте файл `api/contact.php` напрямую:**
   ```php
   $resendApiKey = 're_RfkBrEkZ_7gYxeyyNDhtR6AvYTQBmem3j';
   $recipientEmail = 'info@msk.splitis.ru';
   ```

3. Обновите конфигурацию Nginx (см. раздел ниже)

**Вариант 2: Node.js с PM2**

1. Установите зависимости на сервере:
   ```bash
   npm install
   ```

2. Создайте файл `.env` в корне проекта:
   ```env
   RESEND_API_KEY=re_RfkBrEkZ_7gYxeyyNDhtR6AvYTQBmem3j
   CONTACT_EMAIL=info@msk.splitis.ru
   NODE_ENV=production
   PORT=5000
   ```

3. Установите PM2:
   ```bash
   npm install -g pm2
   ```

4. Запустите сервер:
   ```bash
   pm2 start server.js --name "msk-splitis-api"
   pm2 save
   pm2 startup
   ```

5. Настройте Nginx как reverse proxy (см. раздел ниже)

### Для локальной разработки (server.js)

Создайте файл `.env` в корне проекта (или установите переменные окружения):

```env
RESEND_API_KEY=re_RfkBrEkZ_7gYxeyyNDhtR6AvYTQBmem3j
CONTACT_EMAIL=info@msk.splitis.ru
```

**Важно:** Файл `.env` должен быть добавлен в `.gitignore` и не должен попадать в репозиторий!

### Для Netlify

1. Перейдите в настройки проекта на Netlify
2. Откройте раздел "Environment variables"
3. Добавьте переменные:
   - `RESEND_API_KEY` = `re_RfkBrEkZ_7gYxeyyNDhtR6AvYTQBmem3j`
   - `CONTACT_EMAIL` = `info@msk.splitis.ru` (опционально)

### Для Vercel

1. Перейдите в настройки проекта на Vercel
2. Откройте раздел "Environment Variables"
3. Добавьте переменные:
   - `RESEND_API_KEY` = `re_RfkBrEkZ_7gYxeyyNDhtR6AvYTQBmem3j`
   - `CONTACT_EMAIL` = `info@msk.splitis.ru` (опционально)

## 📁 Структура файлов

### Serverless функции

- **Netlify:** `netlify/functions/contact.js` - функция для Netlify
- **Vercel:** `api/contact.js` - функция для Vercel

### VPS Timeweb

- **PHP вариант:** `api/contact.php` - PHP скрипт для обработки форм (рекомендуется)
- **Node.js вариант:** `server.js` - Node.js сервер с поддержкой `/api/contact`

### Локальная разработка

- **server.js** - обновлен для поддержки `/api/contact` endpoint

### Frontend

- **assets/js/form-handler.js** - обновлен для отправки данных на `/api/contact`

## 🚀 Использование

### VPS Timeweb - Настройка Nginx

**Вариант 1: PHP (рекомендуется для статического сайта)**

1. Убедитесь, что PHP-FPM установлен и запущен:
   ```bash
   sudo systemctl status php8.1-fpm  # или ваша версия PHP
   ```

2. Найдите путь к сокету PHP-FPM:
   ```bash
   sudo find /var/run -name "*fpm.sock"
   # Обычно: /var/run/php/php8.1-fpm.sock
   ```

3. Обновите конфигурацию Nginx (файл `/etc/nginx/sites-available/msk.splitis.ru`):
   - Используйте пример конфигурации из `nginx.conf` в этом проекте
   - Убедитесь, что путь к PHP-FPM сокету правильный
   - Проверьте путь к `contact.php`: `$document_root/../api/contact.php`

4. Проверьте конфигурацию и перезагрузите Nginx:
   ```bash
   sudo nginx -t
   sudo systemctl reload nginx
   ```

5. Проверьте работу:
   ```bash
   curl -X POST https://msk.splitis.ru/api/contact \
     -H "Content-Type: application/json" \
     -d '{"name":"Test","phone":"+79991234567"}'
   ```

**Вариант 2: Node.js с PM2**

1. Установите зависимости и запустите сервер (см. выше)
2. Настройте Nginx как reverse proxy (используйте закомментированный пример в `nginx.conf`)
3. Проверьте работу аналогично

### Локальная разработка

1. Установите зависимости: `npm install`
2. Создайте `.env` файл с переменными окружения (см. выше)
3. Запустите сервер: `npm run server`
4. Формы будут отправлять данные на `http://localhost:5000/api/contact`

### Production (Netlify)

1. Убедитесь, что переменные окружения настроены в Netlify
2. Функция автоматически будет доступна по адресу: `https://msk.splitis.ru/.netlify/functions/contact`
3. Frontend автоматически использует правильный путь

### Production (Vercel)

1. Убедитесь, что переменные окружения настроены в Vercel
2. Функция автоматически будет доступна по адресу: `https://msk.splitis.ru/api/contact`
3. Frontend автоматически использует правильный путь

## 📝 Настройка домена в Resend

**Важно:** Перед использованием в production нужно:

1. Зайти в Resend Dashboard → Domains
2. Добавить домен `msk.splitis.ru`
3. Настроить DNS записи (SPF, DKIM, DMARC) согласно инструкциям Resend
4. Дождаться верификации домена
5. Обновить `from` адрес в функциях с `noreply@msk.splitis.ru` на ваш верифицированный домен

## 🔍 Проверка работы

1. Заполните форму на сайте
2. Отправьте форму
3. Проверьте почту `info@msk.splitis.ru` - должно прийти письмо с заявкой
4. Проверьте консоль браузера (F12) - не должно быть ошибок
5. Проверьте логи сервера (для локальной разработки) или логи платформы (Netlify/Vercel)

## 🐛 Отладка

### Локальная разработка

- Проверьте, что переменная `RESEND_API_KEY` установлена
- Проверьте консоль сервера на наличие ошибок
- Проверьте консоль браузера (F12) на наличие ошибок сети

### Production (VPS Timeweb)

- Проверьте логи PHP-FPM: `sudo tail -f /var/log/php8.1-fpm.log`
- Проверьте логи Nginx: `sudo tail -f /var/log/nginx/msk.splitis.ru.error.log`
- Проверьте, что переменные окружения установлены правильно
- Проверьте, что файл `api/contact.php` доступен и имеет правильные права (644)
- Проверьте, что PHP-FPM может читать файл
- Проверьте, что домен верифицирован в Resend

### Production (Netlify/Vercel)

- Проверьте логи функций в Netlify/Vercel Dashboard
- Проверьте, что переменные окружения установлены правильно
- Проверьте, что домен верифицирован в Resend

## 📊 Отслеживание отправки форм

Формы автоматически отправляют события в:
- **Яндекс.Метрику** - событие `form_submit` (goal: form_submit)
- **Google Analytics** (через GTM) - событие `form_submit`

## 🔒 Безопасность

- API ключ Resend **НЕ ДОЛЖЕН** попадать в репозиторий
- Используйте переменные окружения для хранения секретов
- Добавьте `.env` в `.gitignore`
- Используйте верифицированный домен в production

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте документацию Resend: https://resend.com/docs
2. Проверьте логи сервера/functions
3. Убедитесь, что домен верифицирован в Resend

