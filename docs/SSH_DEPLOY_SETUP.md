# Автоматический деплой через SSH - Настроено ✅

## Что было сделано:

1. ✅ **Установлен Node.js 20.19.5** на сервере
2. ✅ **Создан скрипт автоматического деплоя**: `/usr/local/bin/deploy-msk-splitis.sh`
3. ✅ **Создана директория для репозитория**: `/var/www/msk.splitis.ru/repo`
4. ✅ **Создан скрипт настройки**: `/root/setup-github-webhook.sh`

## Как использовать:

### Вариант 1: Ручной деплой через SSH

После каждого пуша в GitHub, выполните на сервере:

```bash
ssh root@91.200.150.96 '/usr/local/bin/deploy-msk-splitis.sh'
```

Или подключитесь к серверу и запустите:

```bash
ssh -i ~/.ssh/id_ed25519_timeweb root@91.200.150.96
/usr/local/bin/deploy-msk-splitis.sh
```

### Вариант 2: Настройка автоматического деплоя из GitHub

Для автоматического деплоя при пуше в GitHub, выполните на сервере:

```bash
ssh -i ~/.ssh/id_ed25519_timeweb root@91.200.150.96
/root/setup-github-webhook.sh https://github.com/ВАШ_USERNAME/ВАШ_REPO.git
```

**Важно:** Замените `ВАШ_USERNAME` и `ВАШ_REPO` на реальные значения вашего GitHub репозитория.

После этого скрипт:
1. Клонирует репозиторий в `/var/www/msk.splitis.ru/repo`
2. Установит зависимости
3. Выполнит первую сборку
4. Скопирует файлы на сервер

### Вариант 3: Настройка через панель Timeweb Cloud (рекомендуется)

Используйте встроенный автоматический деплой через панель Timeweb Cloud (см. `docs/DEPLOYMENT.md`).

## Структура на сервере:

```
/var/www/msk.splitis.ru/
├── repo/          # Git репозиторий (рабочая копия)
│   ├── .git/
│   ├── src/
│   ├── assets/
│   └── _site/     # Результат сборки
└── _site/         # Финальные файлы для Nginx (деплой)
```

## Что делает скрипт деплоя:

1. Обновляет репозиторий из GitHub (`git fetch` + `git reset`)
2. Устанавливает зависимости (`npm install`)
3. Собирает проект (`npm run build`)
4. Копирует файлы из `repo/_site/` в `_site/` через rsync
5. Устанавливает права доступа для Nginx
6. Перезагружает Nginx

## Логи деплоя:

Логи сохраняются в: `/var/log/deploy-msk-splitis.log`

Просмотр логов:
```bash
ssh root@91.200.150.96 "tail -f /var/log/deploy-msk-splitis.log"
```

## Проверка работы:

После деплоя проверьте:
- ✅ Сайт доступен: `http://msk.splitis.ru`
- ✅ Файлы обновились: `ls -la /var/www/msk.splitis.ru/_site/`
- ✅ Логи без ошибок: `cat /var/log/deploy-msk-splitis.log`

## Следующие шаги:

1. **Укажите URL вашего GitHub репозитория** для настройки автоматического деплоя
2. Или используйте **ручной деплой** через команду выше
3. Или настройте **автоматический деплой через панель Timeweb Cloud**

---

**Готово к использованию!** 🚀

