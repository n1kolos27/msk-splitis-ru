# Генерация изображений через RunPod API

## 🎨 Настройка RunPod для генерации изображений

### Шаг 1: Создание пода на RunPod

#### Подробная инструкция:

1. **Зайдите на RunPod:**

   - Откройте https://www.runpod.io/
   - Войдите в свой аккаунт

2. **Перейдите в Community Cloud:**

   - В меню выберите **"Community Cloud"** или **"Templates"**
   - Это раздел с готовыми шаблонами для генерации изображений

3. **Найдите шаблон:**

   **Рекомендуемые варианты:**

   - 👉 **Stable Diffusion 3 WebUI** (рекомендуется для высокого качества)
   - 👉 **FLUX.1 Image Generator** (альтернатива, очень качественные результаты)

   В поиске введите: `stable diffusion` или `flux` для быстрого поиска

4. **Настройте параметры пода:**

   При выборе шаблона укажите:

   - **GPU:** RTX 4090 (24 ГБ) — достаточно для SD3/FLUX
     - Альтернативы: RTX A6000, A100 (если доступны)
   - **Disk:** 50 ГБ (минимум)
     - Рекомендуется: 100 ГБ для больших моделей
   - **Template:** Оставьте по умолчанию или выберите последнюю версию

5. **Нажмите Deploy:**
   - Подтвердите создание пода
   - Дождитесь запуска (обычно 1-3 минуты)

#### ✅ После успешного запуска:

Через 2 минуты у вас будет:

- **Публичный URL** вида: `https://ваш-id-3000.proxy.runpod.net`
- **Endpoint ID** (для API): `r1c37wn8la` (уже сохранен в документации)
- **Веб-интерфейс** для ручной генерации (опционально)

#### 📝 Что делать дальше:

1. Сохраните публичный URL (если нужен веб-интерфейс)
2. Endpoint ID уже сохранен: `r1c37wn8la`
3. Переходите к Шагу 2 для получения API ключа

### Шаг 2: Получение API ключа

1. Зайдите в **Settings** → **API Keys**
2. Создайте новый API ключ
3. Скопируйте ключ (показывается только один раз)

### Шаг 3: Настройка переменных окружения

**Важно:** Создайте файл `.env` в корне проекта на основе `.env.example`:

1. Скопируйте `.env.example` в `.env`:

   ```bash
   cp .env.example .env
   ```

   Или создайте файл `.env` вручную в корне проекта.

2. Откройте файл `.env` и добавьте ваш реальный API ключ:

```env
# RunPod API для генерации изображений
RUNPOD_API_KEY=ваш_реальный_api_ключ_из_runpod
RUNPOD_ENDPOINT_ID=r1c37wn8la
```

**Формат API ключа:** `RUNPOD_API_KEY_ABC123XYZ456DEF789` (пример формата)

**Важно:**

- ✅ Файл `.env` уже добавлен в `.gitignore` (не попадет в репозиторий)
- ✅ Используйте переменные окружения для безопасности
- ✅ Никогда не коммитьте реальный `.env` файл в репозиторий

**Через секреты в платформе (рекомендуется для production):**

- **Secret Name:** `runpod_api_key`
- **Secret Value:** `RUNPOD_API_KEY_ABC123XYZ456DEF789` (ваш реальный ключ)
- **Описание:** API ключ для доступа к RunPod API и генерации изображений

---

## ✅ Проверка конфигурации

Перед использованием проверьте настройки:

```bash
npm run check:runpod
```

Или напрямую:

```bash
node scripts/check-runpod-config.js
```

Скрипт проверит:

- ✅ Наличие `RUNPOD_API_KEY`
- ✅ Наличие `RUNPOD_ENDPOINT_ID`
- ✅ Формат переменных
- ✅ Доступность API (опционально)

---

## 🚀 Быстрый старт

### Использование скрипта генерации

В проекте есть готовый скрипт для автоматической генерации изображений:

```bash
# Генерация изображений продуктов
npm run generate:images -- --type=products --count=5

# Генерация изображений услуг
npm run generate:images -- --type=services --count=4

# Генерация изображения для статьи блога
npm run generate:images -- --type=blog --article=kak-vybrat-kondicioner
```

Или напрямую через Node.js:

```bash
node scripts/generate-images.js --type=products --count=5
```

Скрипт автоматически:

- ✅ Генерирует изображения через RunPod API
- ✅ Сохраняет в нужные папки (`assets/images/`)
- ✅ Обрабатывает ошибки
- ✅ Показывает прогресс

---

## 📋 Использование API RunPod

### Получение endpoint ID

После создания пода вы получите endpoint ID вида: `r1c37wn8la`

Этот ID используется для:

- Вызова API генерации изображений
- Управления подом
- Мониторинга статуса

### Пример запроса к API

```javascript
const RUNPOD_API_KEY = process.env.RUNPOD_API_KEY;
const ENDPOINT_ID = process.env.RUNPOD_ENDPOINT_ID || "r1c37wn8la";

async function generateImage(prompt) {
  const response = await fetch(`https://api.runpod.io/v2/${ENDPOINT_ID}/run`, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${RUNPOD_API_KEY}`,
    },
    body: JSON.stringify({
      input: {
        prompt: prompt,
        num_inference_steps: 50,
        guidance_scale: 7.5,
        width: 1024,
        height: 1024,
      },
    }),
  });

  return await response.json();
}
```

### Примеры промптов для кондиционеров

**Продукты:**

```
"Professional product photography of Daikin FTXS25K air conditioner, white modern design, clean white background, studio lighting, high quality commercial photo, 8K resolution, sharp focus"
```

**Установка:**

```
"Professional photo of air conditioner installation in modern apartment in Moscow, skilled technician working with tools, realistic interior, natural lighting, high detail, commercial photography style"
```

**Обслуживание:**

```
"Professional photo of air conditioner maintenance service, technician cleaning filters, modern apartment interior in Moscow, natural lighting, realistic, high quality"
```

---

## 🔧 Технические требования

### Форматы изображений

После генерации необходимо:

1. Конвертировать в WebP (85% качество)
2. Создать JPG fallback
3. Оптимизировать размеры:
   - Hero: 1920×1080px
   - Контентные: 800×600px
   - Продукты: 800×600px

### Оптимизация

Используйте инструменты:

- **ImageMagick** для конвертации
- **Sharp** (Node.js) для оптимизации
- **Squoosh** (веб) для ручной оптимизации

---

## 💰 Стоимость

RunPod взимает плату за:

- Время работы GPU пода
- Хранение данных
- Передачу данных

**Рекомендация:** Запускайте под только когда нужно генерировать изображения, затем останавливайте.

---

## 📝 Чек-лист

- [ ] Создан под на RunPod (Stable Diffusion 3 или FLUX)
- [ ] Получен API ключ из настроек (формат: `RUNPOD_API_KEY_...`)
- [ ] API ключ сохранен в секретах платформы (`runpod_api_key`)
- [ ] Endpoint ID сохранен (r1c37wn8la)
- [ ] Переменные окружения настроены
- [ ] Тестовая генерация выполнена успешно
- [ ] Скрипт оптимизации изображений настроен

## 🔗 Подключение к поду

### Информация о вашем поде:

- **Имя пода:** `solid_olive_lark`
- **ID пода:** `uaah0arcspg8v4`
- **HTTP Service:** Port 7860 (Ready) - веб-интерфейс Stable Diffusion
- **API Port:** Port 19123 - для API запросов
- **Direct TCP IP:** `203.57.40.199:10266` (SSH порт 22)

### SSH подключение:

**Через RunPod SSH:**

```bash
ssh uaah0arcspg8v4-64411df4@ssh.runpod.io -i ~/.ssh/id_ed25519
```

**Через Direct TCP (поддерживает SCP & SFTP):**

```bash
ssh root@203.57.40.199 -p 10266 -i ~/.ssh/id_ed25519
```

**На Windows PowerShell:**

```powershell
ssh root@203.57.40.199 -p 10266 -i $env:USERPROFILE\.ssh\id_ed25519
```

### Web Terminal:

**Рекомендация:** Включить "Enable Web Terminal" — удобно для:

- Быстрого доступа к терминалу прямо в браузере
- Отладки без SSH клиента
- Просмотра логов и настройки

**Как включить:**

1. В RunPod → ваш под → вкладка "Connect"
2. Найдите раздел "Web Terminal"
3. Переключите "Enable Web Terminal" в положение "On"
4. После активации появится кнопка для открытия терминала в браузере

---

## 🔐 Безопасность

**КРИТИЧНО:**

- ✅ API ключ RunPod хранится только в секретах платформы
- ✅ Файл `.env` добавлен в `.gitignore`
- ✅ API ключ НЕ должен попадать в репозиторий
- ✅ Используйте переменные окружения для доступа к ключу

**Текущие настройки:**

- **Secret Name:** `runpod_api_key`
- **Endpoint ID:** `r1c37wn8la`
- **Формат ключа:** `RUNPOD_API_KEY_ABC123XYZ456DEF789`

---

## 🔗 Полезные ссылки

- [RunPod Documentation](https://docs.runpod.io/)
- [RunPod API Reference](https://docs.runpod.io/serverless/endpoints/run)
- [Stable Diffusion Guide](https://stability.ai/)
- [FLUX Guide](https://blackforestlabs.ai/)

---

**Последнее обновление:** Январь 2025  
**Endpoint ID:** r1c37wn8la
