# 🚀 Руководство по развертыванию - msk.splitis.ru

**Версия:** 1.0.0  
**Последнее обновление:** Ноябрь 2024

---

## 📋 Содержание

1. [Автоматический деплой через Timeweb Cloud](#автоматический-деплой-через-timeweb-cloud)
2. [Процесс деплоя](#процесс-деплоя)
3. [Ручной деплой](#ручной-деплой)
4. [Решение проблем](#решение-проблем)
5. [Откат к предыдущей версии](#откат-к-предыдущей-версии)

---

## 🔄 Автоматический деплой через Timeweb Cloud

### Преимущества

- ✅ Автоматический деплой при пуше в ветку `main`
- ✅ История всех деплоев в панели управления
- ✅ Возможность отката к предыдущей версии
- ✅ Логи сборки и деплоя доступны в реальном времени
- ✅ Не требует настройки CI/CD вручную

### Настройка (первый раз)

#### Шаг 1: Создание проекта в Timeweb Cloud

1. Войдите в панель [Timeweb Cloud](https://timeweb.cloud/)
2. Перейдите: **Проекты** → **Создать проект**
3. Выберите тип: **"Статический сайт"** или **"Node.js"**
4. Укажите название проекта: `msk.splitis.ru`

#### Шаг 2: Подключение GitHub репозитория

1. Нажмите **"Подключить репозиторий"**
2. Авторизуйтесь через GitHub (если еще не авторизован)
   - Разрешите доступ к репозиториям
   - Выберите репозиторий с проектом msk.splitis.ru
3. Выберите ветку: `main`

#### Шаг 3: Настройка сборки

В разделе **"Настройки сборки"** укажите:

- **Команда установки зависимостей**: `npm install`
- **Команда сборки**: `npm run build`
- **Директория вывода**: `_site`
- **Node.js версия**: `20.x` (рекомендуется) или `18.x`

#### Шаг 4: Настройка деплоя

В разделе **"Настройки деплоя"** укажите:

- **Автоматический деплой**: ✅ Включить
- **Ветка для деплоя**: `main`
- **Путь на сервере**: `/var/www/msk.splitis.ru/_site`
- **Очистка перед деплоем**: ✅ Включить (рекомендуется)

#### Шаг 5: Переменные окружения (если нужны)

Если проект использует переменные окружения (например, для API ключей):

1. Перейдите в раздел **"Переменные окружения"**
2. Добавьте необходимые переменные:
   - `RESEND_API_KEY` (если используется для форм)
   - `CONTACT_EMAIL` (если используется)
   - Другие переменные по необходимости

#### Шаг 6: Сохранение и первый деплой

1. Нажмите **"Сохранить"** или **"Создать проект"**
2. Запустите первый деплой вручную (если есть такая опция)
3. Дождитесь завершения сборки и деплоя
4. Проверьте логи на наличие ошибок

---

## 📦 Процесс деплоя

### Автоматический деплой

После настройки автоматический деплой работает следующим образом:

1. **Push в ветку `main`** → Timeweb Cloud автоматически обнаруживает изменения
2. **Клонирование репозитория** → Клонируется последняя версия из ветки `main`
3. **Установка зависимостей** → Выполняется `npm install`
4. **Сборка проекта** → Выполняется `npm run build`
5. **Деплой файлов** → Содержимое `_site/` копируется на сервер
6. **Проверка** → Проверяется успешность деплоя

### Время деплоя

- **Установка зависимостей**: ~30-60 секунд
- **Сборка проекта**: ~10-30 секунд
- **Копирование файлов**: ~5-10 секунд
- **Общее время**: ~1-2 минуты

### Мониторинг деплоя

В панели Timeweb Cloud доступны:

- **Логи сборки** - вывод команд `npm install` и `npm run build`
- **Логи деплоя** - информация о копировании файлов
- **Статус деплоя** - успешно/ошибка
- **Время деплоя** - сколько времени занял процесс

---

## 🔧 Ручной деплой

Если нужно задеплоить вручную (без автоматизации):

### Вариант 1: Через панель Timeweb Cloud

1. Перейдите в проект `msk.splitis.ru`
2. Нажмите **"Запустить деплой"** или **"Redeploy"**
3. Выберите ветку `main`
4. Дождитесь завершения

### Вариант 2: Через SSH

```bash
# 1. Подключиться к серверу
ssh -i ~/.ssh/id_ed25519_timeweb root@91.200.150.96

# 2. Перейти в рабочую директорию (если есть)
cd /tmp/deploy-msk-splitis-ru

# 3. Обновить код из репозитория
git pull origin main

# 4. Установить зависимости
npm install

# 5. Собрать проект
npm run build

# 6. Скопировать файлы на сервер
rsync -av --delete _site/ /var/www/msk.splitis.ru/_site/

# 7. Установить правильные права доступа
chown -R www-data:www-data /var/www/msk.splitis.ru/_site
```

### Вариант 3: Через SCP (локальная сборка)

```bash
# 1. Собрать проект локально
npm run build

# 2. Загрузить файлы на сервер
scp -i ~/.ssh/id_ed25519_timeweb -r _site/* root@91.200.150.96:/var/www/msk.splitis.ru/_site/

# 3. Установить права доступа (через SSH)
ssh -i ~/.ssh/id_ed25519_timeweb root@91.200.150.96 "chown -R www-data:www-data /var/www/msk.splitis.ru/_site"
```

---

## 🐛 Решение проблем

### Проблема: Ошибка сборки

**Симптомы:**
- Деплой не завершается успешно
- В логах видны ошибки компиляции

**Решение:**
1. Проверьте логи сборки в панели Timeweb Cloud
2. Убедитесь, что все зависимости установлены (`npm install` прошел успешно)
3. Проверьте локально: `npm run build` должен работать без ошибок
4. Убедитесь, что версия Node.js совместима (рекомендуется 20.x)

### Проблема: Файлы не появляются на сервере

**Симптомы:**
- Деплой завершился успешно, но сайт не обновился

**Решение:**
1. Проверьте путь на сервере в настройках проекта
2. Убедитесь, что путь указан правильно: `/var/www/msk.splitis.ru/_site`
3. Проверьте права доступа к директории:
   ```bash
   ssh root@91.200.150.96 "ls -la /var/www/msk.splitis.ru/_site"
   ```
4. Убедитесь, что Nginx имеет доступ к файлам:
   ```bash
   ssh root@91.200.150.96 "chown -R www-data:www-data /var/www/msk.splitis.ru/_site"
   ```

### Проблема: Nginx не видит новые файлы

**Симптомы:**
- Файлы загружены, но сайт показывает старую версию

**Решение:**
1. Проверьте конфигурацию Nginx:
   ```bash
   ssh root@91.200.150.96 "nginx -t"
   ```
2. Перезагрузите Nginx:
   ```bash
   ssh root@91.200.150.96 "systemctl reload nginx"
   ```
3. Очистите кэш браузера или проверьте в режиме инкогнито

### Проблема: Ошибка подключения к GitHub

**Симптомы:**
- Timeweb Cloud не может клонировать репозиторий

**Решение:**
1. Проверьте, что репозиторий публичный или доступен для Timeweb Cloud
2. Переподключите репозиторий в настройках проекта
3. Проверьте права доступа GitHub аккаунта

### Проблема: Медленный деплой

**Симптомы:**
- Деплой занимает слишком много времени

**Решение:**
1. Проверьте размер репозитория и зависимостей
2. Убедитесь, что `node_modules/` не попадает в репозиторий (должен быть в `.gitignore`)
3. Рассмотрите использование кэша зависимостей в настройках проекта

---

## ⏪ Откат к предыдущей версии

### Через панель Timeweb Cloud

1. Перейдите в проект `msk.splitis.ru`
2. Откройте раздел **"История деплоев"** или **"Deployments"**
3. Найдите нужную версию в истории
4. Нажмите **"Откатить"** или **"Redeploy"** рядом с нужной версией
5. Подтвердите откат

### Через Git (рекомендуется)

Если нужно откатить изменения в коде:

```bash
# 1. Найти нужный коммит
git log --oneline

# 2. Откатиться к нужному коммиту
git revert <commit-hash>
# или
git reset --hard <commit-hash>

# 3. Запушить изменения
git push origin main

# 4. Автоматический деплой запустится сам
```

---

## 📊 Мониторинг и метрики

### Проверка успешности деплоя

После каждого деплоя проверьте:

1. ✅ Логи деплоя не содержат ошибок
2. ✅ Сайт доступен по адресу `https://msk.splitis.ru`
3. ✅ Все страницы загружаются корректно
4. ✅ Статические ресурсы (CSS, JS, изображения) загружаются
5. ✅ Нет ошибок в консоли браузера

### Инструменты для проверки

- **Логи Nginx**: `/var/log/nginx/msk.splitis.ru.error.log`
- **Google Search Console**: Проверка индексации после деплоя
- **Lighthouse**: Проверка производительности
- **PageSpeed Insights**: Проверка скорости загрузки

---

## 🔐 Безопасность

### Рекомендации

1. **Не храните секреты в репозитории**
   - Используйте переменные окружения в панели Timeweb Cloud
   - Файлы `.env` должны быть в `.gitignore`

2. **Ограничьте доступ к репозиторию**
   - Используйте приватные репозитории для продакшена
   - Настройте права доступа в GitHub

3. **Регулярно обновляйте зависимости**
   ```bash
   npm audit
   npm audit fix
   ```

4. **Используйте HTTPS**
   - Настройте SSL-сертификаты для всех доменов
   - Используйте Let's Encrypt для автоматического обновления

---

## 📝 Чеклист перед деплоем

Перед каждым деплоем проверьте:

- [ ] Все изменения закоммичены и запушены в ветку `main`
- [ ] Локальная сборка проходит успешно: `npm run build`
- [ ] Нет ошибок в логах: `npm run test` (если есть тесты)
- [ ] Переменные окружения настроены в панели (если нужны)
- [ ] SSL-сертификаты актуальны (если используется HTTPS)

---

## 🔗 Полезные ссылки

- [Документация Timeweb Cloud](https://timeweb.cloud/docs)
- [Документация Eleventy](https://www.11ty.dev/docs/)
- [GitHub Actions для деплоя](https://docs.github.com/en/actions)
- [Документация Nginx](https://nginx.org/ru/docs/)

---

## 📞 Поддержка

Если возникли проблемы с деплоем:

1. Проверьте логи в панели Timeweb Cloud
2. Обратитесь в поддержку Timeweb Cloud через панель управления
3. Проверьте документацию проекта: `README.md`, `DEVELOPER_GUIDE.md`

---

**Последнее обновление:** Ноябрь 2024  
**Версия документации:** 1.0.0

