# 🚀 Настройка 11ty (Eleventy)

**Статус:** ✅ Готово к использованию

## 📦 Установка зависимостей

```bash
npm install
```

Это установит:
- `@11ty/eleventy` - генератор статических сайтов
- `html-minifier` - минификация HTML

## 🏗️ Структура проекта

```
msk-splitis-ru/
├── src/
│   ├── _includes/          # Шаблоны (header, footer)
│   │   ├── header.html
│   │   └── footer.html
│   ├── _layouts/           # Базовые шаблоны
│   │   └── page.html
│   ├── _data/              # Централизованные данные
│   │   ├── site.json       # Общие данные сайта
│   │   ├── contacts.json   # Контактные данные
│   │   └── navigation.json # Навигация
│   └── pages/              # Исходные страницы
│       └── test-page.html  # Тестовая страница
├── assets/                 # CSS, JS, изображения
├── .eleventy.js            # Конфигурация 11ty
└── _site/                  # Сгенерированный сайт (output)
```

## 🎯 Использование

### Сборка проекта

```bash
npm run build
```

Сгенерированный сайт будет в папке `_site/`

### Разработка с live reload

```bash
npm run dev
```

Запустит локальный сервер на `http://localhost:8080` с автоматической перезагрузкой при изменениях.

### Только сервер (без watch)

```bash
npm run serve
```

## 📝 Создание новой страницы

Создайте файл в `src/pages/` с front matter:

```html
---
layout: ../_layouts/page.html
title: Название страницы
description: Описание страницы для SEO
keywords: ключевые слова
---

<div class="container">
  <h1>Контент страницы</h1>
  <p>Ваш HTML контент здесь</p>
</div>
```

## 🔧 Доступные данные

В шаблонах доступны следующие данные:

### site (site.json)
- `site.name` - название компании
- `site.domain` - домен сайта
- `site.title` - заголовок сайта
- `site.description` - описание сайта

### contacts (contacts.json)
- `contacts.phone` - основной телефон
- `contacts.phone2` - дополнительный телефон
- `contacts.phoneFormatted` - телефон для tel: ссылок
- `contacts.email` - email
- `contacts.address` - адрес
- `contacts.telegram` - ссылка на Telegram
- `contacts.whatsapp` - ссылка на WhatsApp

### navigation (navigation.json)
- `navigation.main` - основная навигация
- `navigation.services` - услуги

## 🎨 Шаблоны

### Header

Используется автоматически в layout. Поддерживает:
- Динамическую навигацию из `navigation.json`
- Активное состояние текущей страницы
- Контактные данные из `contacts.json`

### Footer

Используется автоматически в layout. Поддерживает:
- Динамическую навигацию
- Список услуг
- Контактные данные

## 🔄 Фильтры

### formatPhone

Форматирует телефон для tel: ссылок:

```njk
<a href="tel:{{ contacts.phone | formatPhone }}">{{ contacts.phone }}</a>
```

### urlencode

Кодирует строку для URL:

```njk
<a href="?text={{ contacts.address | urlencode }}">Карта</a>
```

### date

Форматирует дату:

```njk
{{ page.date | date }}
```

### absoluteUrl

Создает абсолютный URL:

```njk
<link rel="canonical" href="{{ page.url | absoluteUrl }}">
```

## ⚙️ Конфигурация

Основная конфигурация в `.eleventy.js`:

- **Input:** `src/pages/` - исходные страницы
- **Output:** `_site/` - сгенерированный сайт
- **Includes:** `src/_includes/` - шаблоны
- **Layouts:** `src/_layouts/` - базовые шаблоны
- **Data:** `src/_data/` - данные

## 🚀 Миграция существующих страниц

1. Скопируйте HTML файл в `src/pages/`
2. Добавьте front matter с метаданными:
   ```yaml
   ---
   layout: ../_layouts/page.html
   title: Заголовок страницы
   description: Описание
   ---
   ```
3. Удалите дублированный header и footer из HTML
4. Оставьте только контент страницы
5. Запустите `npm run build` для проверки

## 📚 Дополнительная информация

- [Документация 11ty](https://www.11ty.dev/docs/)
- [Nunjucks синтаксис](https://mozilla.github.io/nunjucks/templating.html)

