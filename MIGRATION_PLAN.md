# 📋 План миграции на 11ty (Eleventy)

**Дата создания:** 4 ноября 2025  
**Статус:** 🔄 Подготовка

---

## 🎯 Цель миграции

Устранение дублирования кода и улучшение масштабируемости проекта через внедрение Static Site Generator (11ty).

---

## 📊 Текущее состояние

- ✅ Git репозиторий инициализирован
- ✅ .gitignore обновлен для 11ty
- ✅ README.md обновлен
- ✅ Конфигурация 11ty подготовлена (.eleventy.js)
- ⏳ Ожидает установки зависимостей

---

## 📦 Этап 1: Установка зависимостей

### Команды:

```bash
# Установить 11ty
npm install --save-dev @11ty/eleventy

# Опционально: плагины для минификации
npm install --save-dev @11ty/eleventy-plugin-syntaxhighlight
npm install --save-dev html-minifier
```

### Файлы для создания:
- `package.json` - добавить скрипты для 11ty
- `.eleventy.js` - уже создан

---

## 🏗️ Этап 2: Создание структуры (1 день)

### Структура папок:

```
msk-splitis-ru/
├── src/
│   ├── _includes/          # Шаблоны (header, footer, layout)
│   │   ├── header.html
│   │   ├── footer.html
│   │   └── base.html
│   ├── _layouts/           # Базовые шаблоны
│   │   └── page.html
│   ├── _data/               # Централизованные данные
│   │   ├── site.json        # Общие данные сайта
│   │   ├── contacts.json    # Контактные данные
│   │   └── navigation.json  # Навигация
│   ├── pages/               # Исходные HTML страницы
│   │   ├── index.html
│   │   ├── o-kompanii.html
│   │   └── ...
│   └── assets/              # CSS, JS, изображения (переместить из корня)
├── _site/                   # Сгенерированный сайт (output)
└── .eleventy.js             # Конфигурация
```

### Действия:
1. Создать структуру папок `src/`
2. Переместить `components/` → `src/_includes/`
3. Создать `src/_data/` для централизованных данных
4. Переместить HTML страницы в `src/pages/`
5. Переместить `assets/` в `src/assets/`

---

## 🔄 Этап 3: Миграция компонентов (1 день)

### Шаг 1: Создать базовый layout

**Файл:** `src/_layouts/page.html`

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} | {{ site.name }}</title>
  <!-- ... остальные meta теги ... -->
</head>
<body>
  {% include "header.html" %}
  
  <main id="main-content">
    {{ content }}
  </main>
  
  {% include "footer.html" %}
</body>
</html>
```

### Шаг 2: Конвертировать компоненты в шаблоны

- `components/header.html` → `src/_includes/header.html`
- `components/footer.html` → `src/_includes/footer.html`
- Использовать Nunjucks синтаксис для переменных

### Шаг 3: Создать централизованные данные

**Файл:** `src/_data/site.json`
```json
{
  "name": "ИП Лагуто Иван Иванович",
  "domain": "msk.splitis.ru",
  "title": "Кондиционеры в Москве"
}
```

**Файл:** `src/_data/contacts.json`
```json
{
  "phone": "+7 (499) 757-57-19",
  "phone2": "+7 (903) 236-60-50",
  "email": "info@msk.splitis.ru",
  "address": "г. Мытищи, ул. Веры Волошиной, д. 19/16, офис 229"
}
```

---

## 📄 Этап 4: Миграция страниц (2-3 дня)

### Подход:

1. **Начать с 5-10 страниц** для тестирования
2. **Конвертировать в шаблоны** используя `page.html` layout
3. **Удалить дублированный код** (header/footer)
4. **Использовать централизованные данные** из `_data/`

### Пример миграции страницы:

**До:**
```html
<!DOCTYPE html>
<html>
<head>...</head>
<body>
  <header class="header">...220+ строк...</header>
  <main>Контент страницы</main>
  <footer class="footer">...90+ строк...</footer>
</body>
</html>
```

**После:**
```html
---
layout: page.html
title: О компании
description: Описание страницы
---

Контент страницы
```

---

## 🎨 Этап 5: Централизация метаданных (1 день)

### Создать файл метаданных:

**Файл:** `src/_data/pages.json`
```json
{
  "index": {
    "title": "Кондиционеры в Москве с установкой",
    "description": "...",
    "ogImage": "..."
  },
  "o-kompanii": {
    "title": "О компании",
    "description": "..."
  }
}
```

### Использовать в шаблонах:

```html
<title>{{ pages[page.fileSlug].title }} | {{ site.name }}</title>
```

---

## ⚡ Этап 6: Оптимизация (1 день)

### Минификация:

1. Установить плагин минификации
2. Настроить в `.eleventy.js`
3. Тестировать результат

### Оптимизация изображений:

1. Добавить плагин для оптимизации
2. Настроить автоматическую конвертацию в WebP
3. Настроить lazy loading

### Автоматическая генерация:

1. Sitemap.xml - автоматически
2. Robots.txt - через шаблон
3. Schema.org - через фильтры

---

## 📝 Скрипты для package.json

```json
{
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "dev": "eleventy --serve --watch"
  }
}
```

---

## ✅ Чеклист миграции

### Подготовка:
- [x] Git репозиторий
- [x] .gitignore обновлен
- [x] README.md обновлен
- [x] Конфигурация 11ty
- [ ] Установить зависимости

### Структура:
- [ ] Создать папки src/
- [ ] Переместить компоненты
- [ ] Создать _data/
- [ ] Переместить страницы

### Компоненты:
- [ ] Создать базовый layout
- [ ] Конвертировать header
- [ ] Конвертировать footer
- [ ] Создать централизованные данные

### Миграция:
- [ ] Мигрировать 5-10 страниц
- [ ] Протестировать
- [ ] Мигрировать остальные страницы
- [ ] Удалить дублированный код

### Оптимизация:
- [ ] Настроить минификацию
- [ ] Оптимизировать изображения
- [ ] Автоматическая генерация sitemap

---

## ⏱️ Временные оценки

- **Этап 1:** 30 минут
- **Этап 2:** 1 день
- **Этап 3:** 1 день
- **Этап 4:** 2-3 дня
- **Этап 5:** 1 день
- **Этап 6:** 1 день

**Общее время:** 6-8 дней

---

## 🚀 После миграции

1. Удалить старые файлы компонентов
2. Обновить документацию
3. Настроить CI/CD для автоматической сборки
4. Обучить команду работе с 11ty

---

**Статус:** 📋 Готов к началу миграции

